interface FilterOptions {
    genresIds?: string,
    release?: string, 
    seasons?: string, 
    duration?: string, 
    rating?: string, 
    query?: string, 
    count: number,
    offset: number, 
    series?: string
}